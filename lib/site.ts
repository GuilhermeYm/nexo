/**
 * O repositório é o produto agora.
 *
 * Não existe uma instância central da Nexo para se cadastrar — o visitante
 * da landing sobe a própria, com as próprias chaves. Por isso todo CTA que
 * antes levava a `/login` ou `/registro` leva para cá. Um só lugar, para o
 * endereço nunca divergir entre Navbar, Footer, Hero e a barra fixa do
 * celular.
 */
export const NEXO_GITHUB_URL = "https://github.com/GuilhermeYm/nexo";
